---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    clusterctl.cluster.x-k8s.io/move: ""
  name: nutanixvirtualhadomains.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: NutanixVirtualHADomain
    listKind: NutanixVirtualHADomainList
    plural: nutanixvirtualhadomains
    shortNames:
    - nvha
    singular: nutanixvirtualhadomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Reference of NutnaixMetro object
      jsonPath: .spec.metroRef
      name: Metro
      type: string
    - description: the vHA domain PC resources are ready or not
      jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixVirtualHADomain is the Schema for the NutanixVirtualHADomains
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              categories:
                description: |-
                  categories are the identifiers (key/value pairs) of the Category PC resources of the vHA domain.
                  This field is not set at CR creation time and the reconciler will create the Category PC resources
                  and set this field with the resources' identifiers (key/value pairs).
                items:
                  properties:
                    key:
                      default: ""
                      description: key is the Key of category in PC.
                      type: string
                    value:
                      default: ""
                      description: value is the category value linked to the category
                        key in PC
                      type: string
                  type: object
                maxItems: 2
                type: array
                x-kubernetes-list-map-keys:
                - value
                x-kubernetes-list-type: map
              metroRef:
                description: metroRef holds reference to the NutanixMetro object corresponding
                  to this vHA domain.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: metroRef.name must not be empty
                  rule: has(self.name) && self.name != ""
              protectionPolicy:
                description: |-
                  protectionPolicy is the identifier (name or uuid) of the ProtectionPolicy PC resource of the vHA domain.
                  This field is not set at CR creation time and the reconciler will create the ProtectionPolicy PC resource
                  and set this field with the resource's identifier (name or uuid).
                properties:
                  name:
                    description: name is the resource name in the PC
                    minLength: 1
                    type: string
                  type:
                    description: Type is the identifier type to use for this resource.
                    enum:
                    - uuid
                    - name
                    type: string
                  uuid:
                    description: uuid is the UUID of the resource in the PC.
                    format: uuid
                    maxLength: 36
                    minLength: 36
                    type: string
                required:
                - type
                type: object
                x-kubernetes-validations:
                - message: '''name'' must be set when type is ''name'', and forbidden
                    otherwise'
                  rule: 'self.type == ''name'' ? has(self.name) : !has(self.name)'
                - message: '''uuid'' must be set when type is ''uuid'', and forbidden
                    otherwise'
                  rule: 'self.type == ''uuid'' ? has(self.uuid) && self.uuid.contains(''-'')
                    : !has(self.uuid)'
              recoveryPlans:
                description: |-
                  recoveryPlans are the identifiers (name or uuid) of the RecoveryPlan PC resources of the vHA domain.
                  This field is not set at CR creation time and the reconciler will create the RecoveryPlan PC resources
                  and set this field with the resources' identifiers (name or uuid).
                items:
                  description: NutanixResourceIdentifier holds the identity of a Nutanix
                    PC resource (cluster, image, subnet, etc.)
                  properties:
                    name:
                      description: name is the resource name in the PC
                      minLength: 1
                      type: string
                    type:
                      description: Type is the identifier type to use for this resource.
                      enum:
                      - uuid
                      - name
                      type: string
                    uuid:
                      description: uuid is the UUID of the resource in the PC.
                      format: uuid
                      maxLength: 36
                      minLength: 36
                      type: string
                  required:
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: '''name'' must be set when type is ''name'', and forbidden
                      otherwise'
                    rule: 'self.type == ''name'' ? has(self.name) : !has(self.name)'
                  - message: '''uuid'' must be set when type is ''uuid'', and forbidden
                      otherwise'
                    rule: 'self.type == ''uuid'' ? has(self.uuid) && self.uuid.contains(''-'')
                      : !has(self.uuid)'
                maxItems: 2
                type: array
            required:
            - metroRef
            type: object
            x-kubernetes-validations:
            - message: protectionPolicy, categories and recoveryPlans must be either
                all set or all unset. When all set, categories and recoveryPlans must
                have two items.
              rule: (!has(self.protectionPolicy) && !has(self.categories) && !has(self.recoveryPlans))
                || (has(self.protectionPolicy) && size(self.categories)==2 && size(self.recoveryPlans)==2)
          status:
            description: NutanixVirtualHADomainStatus defines the state of the NutanixVirtualHADomain
              resource.
            properties:
              conditions:
                description: |-
                  conditions represent the latest states of the vHA domain,
                  including if the vHA domain PC resources (categories, protection policy,
                  and recovery plans) are successfully created or validated.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              ready:
                default: false
                description: |-
                  ready is set to true when the vHA domain PC resources (categories, protection
                  policy, and recovery plan) are valid and ready.
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
