---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    clusterctl.cluster.x-k8s.io/move: ""
  name: nutanixvirtualhadomains.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: NutanixVirtualHADomain
    listKind: NutanixVirtualHADomainList
    plural: nutanixvirtualhadomains
    shortNames:
    - nvha
    singular: nutanixvirtualhadomain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Reference of NutanixMetro object
      jsonPath: .spec.metroRef.name
      name: Metro
      type: string
    - description: the vHA domain PC resources are ready or not
      jsonPath: .status.ready
      name: Ready
      type: boolean
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixVirtualHADomain is the Schema for the NutanixVirtualHADomains
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NutanixVirtualHADomainSpec defines the desired state of NutanixVirtualHADomain.
            properties:
              metroRef:
                description: metroRef is a reference to the NutanixMetro object that
                  this virtual HA domain belongs to.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                    type: string
                type: object
                x-kubernetes-map-type: atomic
                x-kubernetes-validations:
                - message: metroRef.name must not be empty
                  rule: self.name != ""
              movementGroups:
                additionalProperties:
                  description: |-
                    NutanixMovementGroup defines a group of entities that are moved together as part of a
                    virtual HA domain failover or migration. It maps each category to the recovery plan
                    that protects the entities belonging to that category.
                  properties:
                    categoryRecoveryPlans:
                      description: |-
                        categoryRecoveryPlans is the list of category-to-recovery-plan mappings whose
                        member entities belong to this movement group.
                      items:
                        description: |-
                          NutanixCategoryRecoveryPlan maps a category to the recovery plan that protects the
                          entities belonging to that category within a movement group, on a given Prism Element.
                        properties:
                          category:
                            description: category is the category (key/value) whose
                              member entities are protected.
                            properties:
                              key:
                                description: key is the Key of category in PC.
                                type: string
                              value:
                                description: value is the category value linked to
                                  the category key in PC
                                type: string
                            type: object
                          failureDomainRef:
                            description: |-
                              failureDomainRef is a reference to the NutanixFailureDomain object that identifies the
                              Prism Element cluster for this category-to-recovery-plan mapping.
                            properties:
                              name:
                                default: ""
                                description: |-
                                  Name of the referent.
                                  This field is effectively required, but due to backwards compatibility is
                                  allowed to be empty. Instances of this type with an empty value here are
                                  almost certainly wrong.
                                  More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                type: string
                            type: object
                            x-kubernetes-map-type: atomic
                            x-kubernetes-validations:
                            - message: failureDomainRef.name must not be empty
                              rule: self.name != ""
                          recoveryPlan:
                            description: recoveryPlan is the recovery plan associated
                              with the category.
                            properties:
                              name:
                                description: name is the resource name in the PC
                                minLength: 1
                                type: string
                              type:
                                description: Type is the identifier type to use for
                                  this resource.
                                enum:
                                - uuid
                                - name
                                type: string
                              uuid:
                                description: uuid is the UUID of the resource in the
                                  PC.
                                format: uuid
                                maxLength: 36
                                minLength: 36
                                type: string
                            required:
                            - type
                            type: object
                            x-kubernetes-validations:
                            - message: '''name'' must be set when type is ''name'',
                                and forbidden otherwise'
                              rule: 'self.type == ''name'' ? has(self.name) : !has(self.name)'
                            - message: '''uuid'' must be set when type is ''uuid'',
                                and forbidden otherwise'
                              rule: 'self.type == ''uuid'' ? has(self.uuid) && self.uuid.contains(''-'')
                                : !has(self.uuid)'
                        required:
                        - category
                        - failureDomainRef
                        - recoveryPlan
                        type: object
                      minItems: 1
                      type: array
                      x-kubernetes-list-type: atomic
                  required:
                  - categoryRecoveryPlans
                  type: object
                description: |-
                  movementGroups defines the named groups of entities that move together within this
                  virtual HA domain. Each key is a user-defined group name (for example "default") and
                  the value describes the entities belonging to that group.
                type: object
              protectionGroup:
                description: protectionGroup identifies the protection policy applied
                  to this virtual HA domain.
                properties:
                  protectionPolicy:
                    description: protectionPolicy identifies the protection policy
                      applied to this virtual HA domain.
                    properties:
                      name:
                        description: name is the resource name in the PC
                        minLength: 1
                        type: string
                      type:
                        description: Type is the identifier type to use for this resource.
                        enum:
                        - uuid
                        - name
                        type: string
                      uuid:
                        description: uuid is the UUID of the resource in the PC.
                        format: uuid
                        maxLength: 36
                        minLength: 36
                        type: string
                    required:
                    - type
                    type: object
                    x-kubernetes-validations:
                    - message: '''name'' must be set when type is ''name'', and forbidden
                        otherwise'
                      rule: 'self.type == ''name'' ? has(self.name) : !has(self.name)'
                    - message: '''uuid'' must be set when type is ''uuid'', and forbidden
                        otherwise'
                      rule: 'self.type == ''uuid'' ? has(self.uuid) && self.uuid.contains(''-'')
                        : !has(self.uuid)'
                required:
                - protectionPolicy
                type: object
            required:
            - metroRef
            type: object
          status:
            description: NutanixVirtualHADomainStatus defines the observed state of
              NutanixVirtualHADomain.
            properties:
              conditions:
                description: conditions represent the latest states of the virtual
                  HA domain.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              movementGroups:
                additionalProperties:
                  description: NutanixMovementGroupStatus captures the observed state
                    of a movement group within a virtual HA domain.
                  properties:
                    ready:
                      default: false
                      description: ready is set to true when the movement group PC
                        resources (categories, recovery plans) are valid and ready.
                      type: boolean
                  required:
                  - ready
                  type: object
                description: |-
                  movementGroups captures the observed state of each movement group defined in the
                  spec, keyed by the movement group name.
                type: object
              ready:
                default: false
                description: |-
                  ready is set to true when the vHA domain PC resources (categories, protection
                  policy, and recovery plan) are valid and ready.
                type: boolean
            required:
            - ready
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
