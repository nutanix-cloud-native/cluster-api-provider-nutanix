---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: nutanixclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: NutanixClusterTemplate
    listKind: NutanixClusterTemplateList
    plural: nutanixclustertemplates
    singular: nutanixclustertemplate
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixClusterTemplate is the Schema for the nutanixclustertemplates
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: NutanixClusterTemplateSpec defines the desired state of NutanixClusterTemplate
            properties:
              template:
                description: 'INSERT ADDITIONAL SPEC FIELDS - desired state of cluster
                  Important: Run "make" to regenerate code after modifying this file'
                properties:
                  spec:
                    description: NutanixClusterSpec defines the desired state of NutanixCluster
                    properties:
                      controlPlaneEndpoint:
                        description: ControlPlaneEndpoint represents the endpoint
                          used to communicate with the control plane. host can be
                          either DNS name or ip address
                        properties:
                          host:
                            description: The hostname on which the API server is serving.
                            type: string
                          port:
                            description: The port on which the API server is serving.
                            format: int32
                            type: integer
                        required:
                        - host
                        - port
                        type: object
                      prismCentral:
                        description: prismCentral holds the endpoint address and port
                          to access the Nutanix Prism Central. When a cluster-wide
                          proxy is installed, by default, this endpoint will be accessed
                          via the proxy. Should you wish for communication with this
                          endpoint not to be proxied, please add the endpoint to the
                          proxy spec.noProxy list.
                        properties:
                          additionalTrustBundle:
                            description: AdditionalTrustBundle is a PEM encoded x509
                              cert for the RootCA that was used to create the certificate
                              for a Prism Central that uses certificates that were
                              issued by a non-publicly trusted RootCA. The trust bundle
                              is added to the cert pool used to authenticate the TLS
                              connection to the Prism Central.
                            properties:
                              data:
                                description: Data of the trust bundle if Kind is String.
                                type: string
                              kind:
                                description: Kind of the Nutanix trust bundle
                                enum:
                                - String
                                - ConfigMap
                                type: string
                              name:
                                description: Name of the credential.
                                type: string
                              namespace:
                                description: namespace of the credential.
                                type: string
                            required:
                            - kind
                            type: object
                          address:
                            description: address is the endpoint address (DNS name
                              or IP address) of the Nutanix Prism Central or Element
                              (cluster)
                            maxLength: 256
                            type: string
                          credentialRef:
                            description: Pass credential information for the target
                              Prism instance
                            properties:
                              kind:
                                description: Kind of the Nutanix credential
                                enum:
                                - Secret
                                type: string
                              name:
                                description: Name of the credential.
                                minLength: 1
                                type: string
                              namespace:
                                description: namespace of the credential.
                                type: string
                            required:
                            - kind
                            - name
                            type: object
                          insecure:
                            default: false
                            description: use insecure connection to Prism endpoint
                            type: boolean
                          port:
                            default: 9440
                            description: port is the port number to access the Nutanix
                              Prism Central or Element (cluster)
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        required:
                        - address
                        - port
                        type: object
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
