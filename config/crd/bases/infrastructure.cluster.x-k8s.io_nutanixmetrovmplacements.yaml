---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  labels:
    clusterctl.cluster.x-k8s.io/move: ""
  name: nutanixmetrovmplacements.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: NutanixMetroVMPlacement
    listKind: NutanixMetroVMPlacementList
    plural: nutanixmetrovmplacements
    singular: nutanixmetrovmplacement
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: VM Placement Strategy
      jsonPath: .spec.placementStrategy
      name: PlacementStrategy
      type: string
    - description: Preferred failureDomain for Preferred Strategy
      jsonPath: .spec.preferredFailureDomain
      name: PreferredFailureDomain
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixMetroVMPlacement is the Schema for the NutanixMetroVMPlacement
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NutanixMetroVMPlacementSpec defines the desired state of
              NutanixMetroVMPlacement
            properties:
              failureDomains:
                description: failureDomains are the references to the two failure
                  domains of the Nutanix Metro Domain.
                items:
                  description: |-
                    LocalObjectReference contains enough information to let you locate the
                    referenced object inside the same namespace.
                  properties:
                    name:
                      default: ""
                      description: |-
                        Name of the referent.
                        This field is effectively required, but due to backwards compatibility is
                        allowed to be empty. Instances of this type with an empty value here are
                        almost certainly wrong.
                        More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      type: string
                  type: object
                  x-kubernetes-map-type: atomic
                maxItems: 2
                minItems: 2
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              placementStrategy:
                default: Preferred
                description: placementStrategy defines the VM placement strategy of
                  Preferred or Random, with Preferred as the default.
                enum:
                - Preferred
                - Random
                type: string
              preferredFailureDomain:
                description: |-
                  preferredFailureDomain is the preferred failureDomain name, specifying the failure domain the VM should be provisioned.
                  When the preferred failure domain is not available, or being evacuated (e.g. in maintainence), the VM
                  will be provisioned to the remaining failure domain.
                  This field is required when the VM placement strategy is Preferred.
                type: string
            required:
            - failureDomains
            - placementStrategy
            type: object
            x-kubernetes-validations:
            - message: preferredFailureDomain is required for Preferred placementStrategy,
                and it must be referenced in spec.failureDomains.
              rule: self.placementStrategy != "Preferred" || (has(self.preferredFailureDomain)
                && self.failureDomains.exists(fd, fd.name == self.preferredFailureDomain))
          status:
            description: NutanixMetroVMPlacementStatus defines the observed state
              of NutanixMetroVMPlacement resource.
            properties:
              conditions:
                description: conditions represent the latest states of the NutanixMetroVMPlacement.
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This field may be empty.
                      maxLength: 10240
                      minLength: 1
                      type: string
                    reason:
                      description: |-
                        reason is the reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may be empty.
                      maxLength: 256
                      minLength: 1
                      type: string
                    severity:
                      description: |-
                        severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      maxLength: 32
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      maxLength: 256
                      minLength: 1
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              v1beta2:
                description: v1beta2 groups all the fields that will be added or modified
                  in NutanixMetroVMPlacement's status with the v1beta2 version.
                properties:
                  conditions:
                    description: conditions represents the observations of a NutanixMetroVMPlacement's
                      current state.
                    items:
                      description: Condition contains details for one aspect of the
                        current state of this API Resource.
                      properties:
                        lastTransitionTime:
                          description: |-
                            lastTransitionTime is the last time the condition transitioned from one status to another.
                            This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                          format: date-time
                          type: string
                        message:
                          description: |-
                            message is a human readable message indicating details about the transition.
                            This may be an empty string.
                          maxLength: 32768
                          type: string
                        observedGeneration:
                          description: |-
                            observedGeneration represents the .metadata.generation that the condition was set based upon.
                            For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                            with respect to the current state of the instance.
                          format: int64
                          minimum: 0
                          type: integer
                        reason:
                          description: |-
                            reason contains a programmatic identifier indicating the reason for the condition's last transition.
                            Producers of specific condition types may define expected values and meanings for this field,
                            and whether the values are considered a guaranteed API.
                            The value should be a CamelCase string.
                            This field may not be empty.
                          maxLength: 1024
                          minLength: 1
                          pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                          type: string
                        status:
                          description: status of the condition, one of True, False,
                            Unknown.
                          enum:
                          - "True"
                          - "False"
                          - Unknown
                          type: string
                        type:
                          description: type of condition in CamelCase or in foo.example.com/CamelCase.
                          maxLength: 316
                          pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                          type: string
                      required:
                      - lastTransitionTime
                      - message
                      - reason
                      - status
                      - type
                      type: object
                    maxItems: 32
                    type: array
                    x-kubernetes-list-map-keys:
                    - type
                    x-kubernetes-list-type: map
                type: object
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
