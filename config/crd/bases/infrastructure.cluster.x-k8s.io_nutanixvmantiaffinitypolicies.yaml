---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: nutanixvmantiaffinitypolicies.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: NutanixVMAntiAffinityPolicy
    listKind: NutanixVMAntiAffinityPolicyList
    plural: nutanixvmantiaffinitypolicies
    singular: nutanixvmantiaffinitypolicy
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.name
      name: Name
      type: string
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixVMAntiAffinityPolicy is the Schema for the nutanixvmaffinitypolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NutanixVMAntiAffinityPolicySpec defines the desired state
              of NutanixVMAntiAffinityPolicy
            properties:
              categories:
                description: Category of the policy
                items:
                  properties:
                    key:
                      description: key is the Key of category in PC.
                      type: string
                    value:
                      description: value is the category value linked to the category
                        key in PC
                      type: string
                  type: object
                type: array
              description:
                description: Description of the policy
                type: string
              name:
                description: Name of the policy
                type: string
            type: object
          status:
            description: NutanixVMAntiAffinityPolicyStatus defines the observed state
              of NutanixVMAntiAffinityPolicy
            properties:
              categoriesCleanupList:
                description: CleanupList for categories that need to be cleaned up
                items:
                  properties:
                    key:
                      description: key is the Key of category in PC.
                      type: string
                    value:
                      description: value is the category value linked to the category
                        key in PC
                      type: string
                  type: object
                type: array
              cleanupPolicy:
                description: CleanupPolicy indicates if the policy should be cleaned
                  up
                type: boolean
              conditions:
                description: Conditions of the policy
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              message:
                description: Message of the policy
                type: string
              uuid:
                description: UUID of the policy
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
