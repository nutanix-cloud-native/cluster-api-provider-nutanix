---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: nutanixvmantiaffinitypolicies.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    kind: NutanixVMAntiAffinityPolicy
    listKind: NutanixVMAntiAffinityPolicyList
    plural: nutanixvmantiaffinitypolicies
    singular: nutanixvmantiaffinitypolicy
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - description: Policy created in Prism
      jsonPath: .status.ready
      name: Ready
      type: string
    - description: UUID of the policy in Prism
      jsonPath: .status.policyUUID
      name: PolicyUUID
      type: string
    - description: Number of compliant VMs
      jsonPath: .status.numCompliantVms
      name: CompliantVMs
      type: integer
    - description: Time duration since creation of NutanixVMAntiAffinityPolicy
      jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: NutanixVMAntiAffinityPolicy is the Schema for the nutanixvmantiaffinitypolicies
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NutanixVMAntiAffinityPolicySpec defines the desired state
              of NutanixVMAntiAffinityPolicy
            properties:
              categories:
                description: Categories is the list of categories configured for the
                  VM-VM anti-affinity policy
                items:
                  properties:
                    key:
                      description: key is the Key of category in PC.
                      type: string
                    value:
                      description: value is the category value linked to the category
                        key in PC
                      type: string
                  type: object
                type: array
              description:
                description: Description is the description of the anti-affinity policy
                type: string
              identityRef:
                description: IdentityRef is a reference to the NutanixPrismIdentity
                  object
                type: string
              name:
                description: Name is the name of the anti-affinity policy in Prism
                type: string
            required:
            - identityRef
            - name
            type: object
          status:
            description: NutanixVMAntiAffinityPolicyStatus defines the observed state
              of NutanixVMAntiAffinityPolicy
            properties:
              conditions:
                description: Conditions defines current service state of the NutanixVMAntiAffinityPolicy
                items:
                  description: Condition defines an observation of a Cluster API resource
                    operational state.
                  properties:
                    lastTransitionTime:
                      description: |-
                        Last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed. If that is not known, then using the time when
                        the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A human readable message indicating details about the transition.
                        This field may be empty.
                      type: string
                    reason:
                      description: |-
                        The reason for the condition's last transition in CamelCase.
                        The specific API may choose whether or not this field is considered a guaranteed API.
                        This field may not be empty.
                      type: string
                    severity:
                      description: |-
                        Severity provides an explicit classification of Reason code, so the users or machines can immediately
                        understand the current situation and act accordingly.
                        The Severity field MUST be set only when Status=False.
                      type: string
                    status:
                      description: Status of the condition, one of True, False, Unknown.
                      type: string
                    type:
                      description: |-
                        Type of condition in CamelCase or in foo.example.com/CamelCase.
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions
                        can be useful (see .node.status.conditions), the ability to deconflict is important.
                      type: string
                  required:
                  - lastTransitionTime
                  - status
                  - type
                  type: object
                type: array
              createTime:
                description: CreateTime is the VM-VM anti-affinity policy creation
                  time
                format: date-time
                type: string
              failureMessage:
                description: |-
                  FailureMessage will be set in the event that there is a terminal problem
                  reconciling the state and will be set to a descriptive error message.
                type: string
              numCompliantVms:
                description: NumCompliantVms is the number of compliant VMs which
                  are part of the VM-VM anti-affinity policy
                format: int64
                type: integer
              numNonCompliantVms:
                description: NumNonCompliantVms is the number of non-compliant VMs
                  which are part of the VM-VM anti-affinity policy
                format: int64
                type: integer
              numPendingVms:
                description: NumPendingVms is the number of VMs with compliance state
                  as pending, which are part of the VM-VM anti-affinity policy
                format: int64
                type: integer
              policyUUID:
                description: PolicyUUID is the UUID of the anti-affinity policy in
                  Prism
                type: string
              ready:
                description: Ready denotes that the anti-affinity policy has been
                  successfully created or updated
                type: boolean
              updateTime:
                description: UpdateTime is the VM-VM anti-affinity policy last updated
                  time
                format: date-time
                type: string
              vms:
                description: VMs is the list of VM identifiers managed by this policy
                items:
                  description: NutanixResourceIdentifier holds the identity of a Nutanix
                    PC resource (cluster, image, subnet, etc.)
                  properties:
                    name:
                      description: name is the resource name in the PC
                      type: string
                    type:
                      description: Type is the identifier type to use for this resource.
                      enum:
                      - uuid
                      - name
                      type: string
                    uuid:
                      description: uuid is the UUID of the resource in the PC.
                      type: string
                  required:
                  - type
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
