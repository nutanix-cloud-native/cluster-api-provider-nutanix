apiVersion: v1
kind: Service
metadata:
  name: webhook-service
  labels:
    control-plane: controller-manager
spec:
  ports:
  - port: 9443
    protocol: TCP
    targetPort: 9443
    name: webhook
  selector:
    control-plane: controller-manager
