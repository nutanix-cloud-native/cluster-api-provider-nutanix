# CVE-2026-34040: Docker AuthZ plugin bypass with oversized request bodies (CVSS 8.8)
# github.com/docker/docker v28.5.2+incompatible — indirect test-only dependency
# pulled in via sigs.k8s.io/cluster-api/test → test/infrastructure/container.
# Fix requires v29.5.3 which uses new module paths (github.com/moby/moby/client);
# no v28.x backport exists. Waiting on upstream cluster-api to migrate.
# This project does not use Docker AuthZ plugins.
CVE-2026-34040

# CVE-2026-34986: go-jose v2 panic in JWE key-wrap decrypt path
# gopkg.in/square/go-jose.v2 v2.6.0 — indirect dependency via k8s.io/apiserver / k8s.io/cluster-bootstrap.
# v2.6.0 is the latest v2 tag; upstream fix exists only in go-jose v3.0.5/v4.1.4 lines.
# Waiting on upstream Kubernetes/cluster-api dependency migration.
CVE-2026-34986
